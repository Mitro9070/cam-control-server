# План развертывания на этом ПК (чеклист)

## Правила работы
- Отмечаю пункт как `[x]` только после фактического выполнения на этом ПК.
- Если шаг блокируется, фиксирую причину и следующий шаг.

## 0) Что уже проверено
- [x] Изучена документация проекта, включая `07_Deployment_and_Ops`.
- [x] Проверена сетевая доступность камеры `192.168.31.234` (HTTP `200 OK`).
- [x] Подтверждено, что устройство отвечает как `greateyes` (веб-страница камеры).
- [x] Снято текущее состояние окружения (python/msbuild/ascom/dotnet/node).

## 1) Подготовка окружения Windows
- [x] Установить `ASCOM Platform 7.x`.
- [x] Установить `Visual Studio Build Tools 2022` (компонент с `MSBuild`).
- [x] Установить `Python 3.11+` и добавить в `PATH`.
- [x] Проверить наличие `python --version` и что это реальный интерпретатор, а не Store alias.
- [x] (Опционально для тестов) Установить `dotnet SDK`.
- [x] (Опционально для UI smoke) Установить `node` и `npm`.

## 2) Базовый деплой Project_R1
- [x] Открыть PowerShell от имени администратора.
- [x] Перейти в `c:\Project\Project_R1\07_Deployment_and_Ops`.
- [x] Выполнить `.\deploy_camera_pc.ps1`.
- [x] Убедиться, что C# драйвер собран (`Release`) и COM-регистрация выполнена.
- [x] Убедиться, что создано/актуально виртуальное окружение `.venv`.
- [x] Убедиться, что применены миграции БД (`alembic upgrade head`).

## 3) Конфигурация сервиса
- [x] Проверить файл `c:\Project\Project_R1\02_Python_Camera_Service\.env`.
- [x] Установить `SDK_MODE=native` для работы с реальной камерой.
- [x] Заполнить `SDK_DLL_PATH` корректным путем к DLL SDK.
- [x] Проверить `CAMERA_INDEX` (обычно `0`, при необходимости скорректировать).
- [x] Проверить параметры хранения (`STORAGE_MODE`, DB-параметры или local fallback).
 
Используется `SDK_DLL_PATH=C:\Program Files\greateyes\greateyesVision\greateyes.dll` (проверены необходимые экспортируемые функции SDK, включая `ConnectCamera` и `StartMeasurement`).

## 4) Запуск и первичная проверка
- [x] Выполнить `.\start_camera_service.ps1`.
- [x] Проверить health: `http://127.0.0.1:3037/api/v1/health`.
- [x] Проверить UI: `http://127.0.0.1:3037/ui/`.
- [x] Проверить, что в `ASCOM Chooser` виден `GreatEyes 9.0 (Project_R1 ASCOM Camera)`.

## 5) Проверка связи с камерой
- [x] Выполнить подключение камеры через API (`/api/v1/camera/connect`).
- [x] Проверить получение state/capabilities (`/api/v1/camera/state`, `/api/v1/camera/capabilities`).
- [x] Запустить тестовую экспозицию и проверить статус/получение кадра.
- [x] Проверить базовые операции охлаждения (если поддерживается камерой).

Статус: обнаружение камеры, capabilities, экспозиция и получение кадра в native-режиме работают.
Статус: операции охлаждения работают после установки `COOLING_HARDWARE_OPTION=42223` и fallback на `TemperatureControl_SetTemperatureLevel`.

## 6) Приемочный минимум (по runbook)
- [ ] Прогнать Python тесты: `python -m pytest -q`.
- [ ] Прогнать C# unit тесты (если установлен `dotnet`).
- [ ] Прогнать UI smoke (`npm run test:ui-smoke`, если установлен `node`).
- [ ] Зафиксировать итог: `Go/No-Go` и список блокеров (если есть).

---

## Журнал выполнения
- 2026-03-07: Создан чеклист развертывания для этого ПК.
- 2026-03-07: Установлены `Python 3.11`, `Visual Studio Build Tools 2022 (MSBuild)`, `dotnet SDK 8`, `Node.js LTS`.
- 2026-03-07: Установлен `ASCOM Platform 7.1 Update 3`; подтвержден `PlatformVersion=7.1` в `HKLM\SOFTWARE\WOW6432Node\ASCOM`.
- 2026-03-07: Добавлены пути Python в `PATH`; `python --version` возвращает `Python 3.11.9`.
- 2026-03-07: Выполнен `deploy_camera_pc.ps1` с повышенными правами; проверены артефакты (`ASCOM.ProjectR1.Camera.dll`, регистрация `ASCOM.ProjectR1.Camera`, `.venv`, ревизия Alembic `20260305_0001`).
- 2026-03-07: По пункту 3 подтверждены `SDK_MODE=native`, `CAMERA_INDEX=0`, параметры хранилища; требуется путь к SDK DLL (`SDK_DLL_PATH`).
- 2026-03-07: Найдена установка `greateyesVision version 8` в `C:\Program Files\greateyes\greateyesVision\`; выбран и установлен `SDK_DLL_PATH` на `greateyes.dll`.
- 2026-03-07: Выполнен native smoke через API: `health` и `ui` = OK, но `camera/connect` возвращает `SdkErrorCode.SDK_CALL_FAILED: ConnectCamera failed: no camera detected`.
- 2026-03-07: Выполнена полная диагностика `greateyesVision`/SDK; подтвержден integration gap для сетевого режима камеры (см. `03_Отчет_нативные_тесты_с_камерой_2026-03-07.md`).
- 2026-03-07: Доработан native-адаптер (`sdk_adapter.py`) и конфиг (`SDK_CAMERA_ADDRESS`, `SDK_CAMERA_INTERFACE`, `SDK_CAMERA_PORT`), добавлена автосинхронизация `greateyes.ini`; повторный native smoke: `ConnectCamera failed: no camera detected`.
- 2026-03-07: Проверена гипотеза занятости камерой `greateyesVision` (приложение закрыто, соединение освобождено) — ошибка сохраняется.
- 2026-03-07: Добавлен инженерный протокол `04_Инженерный_протокол_интеграции_GreatEyes_SDK.md` и утилита `tools/probe_greateyes_sdk.py` для изолированной диагностики SDK.
- 2026-03-08: В `repos` найден `greateyes.h` с точными прототипами SDK; внедрена Ethernet-последовательность `SetupCameraInterface -> ConnectToSingleCameraServer -> ConnectCamera -> InitCamera`.
- 2026-03-08: Native повторный прогон: `connect/capabilities` — OK, повторные `connect/disconnect` — OK; блокеры: экспозиция/кадр и охлаждение.
- 2026-03-08: Доработан acquisition path (`SetExposure`, fallback для `*_DynBitDepth`, timeout в monitor); экспозиция завершается `completed`, `images/latest` возвращает кадр (2048x2052).
- 2026-03-08: Добавлены post-connect настройки SDK (`SetBusyTimeout`, `SetReadOutSpeed`, `SetupSensorOutputMode`, `SetBitDepth`); connect/disconnect стабилизирован.
- 2026-03-08: Для охлаждения найден рабочий профиль (`COOLING_HARDWARE_OPTION=42223` из `repos`); `cooling/target` и `cooling/status` проходят в native-режиме.
