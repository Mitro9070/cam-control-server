# Отчет: приемочный прогон (2026-03-08)

Контур: `Project_R1` на текущем ПК  
Режим SDK: `native`  
Цель: выполнить приемочный минимум (`pytest`, C# unit, UI smoke, live API smoke) и зафиксировать статус.

## 1) Что запускалось

1. Python тесты:
   - команда: `.\.venv\Scripts\python.exe -m pytest`
2. C# unit:
   - команда: `dotnet test 01_ASCOM_Local_Server\ASCOM.ProjectR1.LocalServer.sln -c Release`
3. UI smoke:
   - команда: `npm run test:ui-smoke` в `06_Testing`
4. Live native API smoke (ручной сценарий через HTTP):
   - `health`, `settings`
   - `camera/connect`, `camera/state`, `camera/capabilities`
   - `camera/exposures`, `status`, `images/latest`
   - `camera/cooling/status`, `camera/cooling/target`, `camera/cooling/warmup`
   - `logs/events`, `camera/disconnect`

## 2) Результаты

### 2.1 Python (`pytest`)
- Статус: **PASS**
- Результат: `12 passed, 2 warnings`
- Замечание: warnings про `FastAPI on_event` deprecation (не блокер прогона).

### 2.2 C# unit (`dotnet test`)
- Статус: **PARTIAL / NOT APPLICABLE**
- Факт: решение собирается, но тестовые проекты в `01_ASCOM_Local_Server` не обнаружены (фактически `dotnet test` не выполнил unit-тесты).
- Вывод: пункт "C# unit" как часть приемочного минимума формально не закрыт из-за отсутствия тестов в решении.

### 2.3 UI smoke (Playwright)
- Статус: **PASS**
- Результат: `1 passed`
- Проверенный сценарий: `connect -> exposure -> image -> logs`.

### 2.4 Live native API smoke
- `GET /api/v1/health` -> **OK**
- `POST /api/v1/camera/connect` -> **OK** (`GE 2048 2048 BI NIMO`)
- `GET /api/v1/camera/capabilities` -> **OK**
- `POST /api/v1/camera/exposures` -> **OK**
- `GET /api/v1/camera/exposures/{id}/status` -> в момент проверки было `exposing`
- `GET /api/v1/camera/images/latest` -> **OK** (кадр возвращается)
- `GET /api/v1/camera/cooling/status` -> **OK** (при повторной проверке в connected state)
- `PUT /api/v1/camera/cooling/target` -> **OK** (при повторной проверке в connected state)
- `POST /api/v1/camera/cooling/warmup` и `GET /.../warmup/{id}` -> **OK** (`running`)
- `GET /api/v1/logs/events` -> **OK**
- `POST /api/v1/camera/disconnect` -> **OK**

## 3) Наблюдения и риски

1. В одном из прогонов live smoke были transient-ошибки на cooling endpoint'ах (`500/400`), которые не воспроизвелись на повторной прямой проверке в том же сеансе.
2. На коротком polling экспозиции (`~1.2s`) статус мог оставаться `exposing`, при этом `images/latest` возвращал доступный последний кадр (возможна гонка между завершением экспозиции и чтением latest).
3. Системный риск по native SDK, зафиксированный ранее, остается: интермиттентный `camera busy` в отдельных сеансах.
4. Формальный приемочный подпункт "C# unit tests" не закрыт из-за отсутствия test-проектов в `01_ASCOM_Local_Server`.

## 4) Итоговый статус Go/No-Go

- **Рекомендация: CONDITIONAL GO**
  - Для ручной эксплуатации UI/API и продолжения интеграции — **GO**.
  - Для формального полного закрытия приемочного минимума по чеклисту — **NO-GO до закрытия двух пунктов**:
    1) добавить/запустить C# unit tests;
    2) подтвердить стабильность native/cooling на длительном прогоне без transient сбоев.

## 5) Артефакты прогона

- `02_Python_Camera_Service`:
  - `pytest` (12 passed)
- `06_Testing`:
  - Playwright UI smoke (1 passed)
- Live API проверки:
  - успешные ответы `connect/capabilities/cooling/warmup/disconnect` зафиксированы в текущем сеансе.
