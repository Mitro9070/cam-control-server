# Полный план работ и статус (на текущем ПК)

Дата актуализации: 2026-03-08

## A. Инфраструктура и деплой
- [x] Проверить документацию проекта, включая `07_Deployment_and_Ops`.
- [x] Проверить доступность камеры по сети (`192.168.31.234`).
- [x] Установить prerequisites (`ASCOM`, `BuildTools`, `Python`, `dotnet`, `node`).
- [x] Выполнить базовый деплой (`deploy_camera_pc.ps1`).
- [x] Проверить COM-регистрацию ASCOM драйвера.
- [x] Подготовить `.venv`, зависимости и миграции.

## B. Конфигурация native режима
- [x] Подключить реальную DLL SDK (`greateyes.dll`) в `.env`.
- [x] Включить `SDK_MODE=native`.
- [x] Добавить и заполнить сетевые параметры SDK:
  - [x] `SDK_CAMERA_ADDRESS`
  - [x] `SDK_CAMERA_INTERFACE`
  - [x] `SDK_CAMERA_PORT`
  - [x] `SDK_READOUT_SPEED`
  - [x] `SDK_SENSOR_OUTPUT_MODE`
  - [x] `SDK_BIT_DEPTH`
  - [x] `SDK_BUSY_TIMEOUT_MS`

## C. Интеграция SDK (код)
- [x] Провести диагностику экспортов `greateyes.dll`.
- [x] Найти прототипы SDK в `repos` (`greateyes.h`).
- [x] Вшить корректные сигнатуры ключевых функций в `sdk_adapter.py`.
- [x] Вшить корректный порядок Ethernet-подключения:
  - [x] `SetupCameraInterface`
  - [x] `ConnectToSingleCameraServer` (с fallback)
  - [x] `ConnectCamera`
  - [x] `InitCamera`
- [x] Добавить очистку stale server session (`DisconnectCameraServer`).
- [x] Стабилизировать повторные `connect/disconnect`.
- [x] Улучшить acquisition path:
  - [x] `SetExposure` перед стартом экспозиции
  - [x] fallback между `DynBitDepth` и классическим API чтения
  - [x] timeout-защита в мониторинге экспозиции

## D. Фактические результаты native тестов
- [x] `health` -> OK
- [x] `camera/connect` -> OK (камера определяется: `GE 2048 2048 BI NIMO`)
- [x] `camera/capabilities` -> OK
- [x] `camera/exposures` -> завершение экспозиции `completed`
- [x] `camera/images/latest` -> кадр получен (`2048x2052`, base64 данные присутствуют)
- [x] `camera/cooling/target` -> целевая температура применена

## E. Открытые риски
- [ ] Подтвердить стабильность охлаждения на длительном прогоне (не только короткий smoke).
- [x] Уточнить бизнес-правило маппинга `target_temp_c` в level-mode (вендорский SDK использует уровни охлаждения). Правило зафиксировано в `техническое-решение-greateyes/01_ТР_GreatEyes_ASCOM_Python_PostgreSQL.md` (раздел `7.3.1`).

## F. Следующие шаги (до полного закрытия работ)
- [x] Вытащить корректный `cooling_hardware_option` из вендорской документации/SDK-пакета для модели `GE 2048 2048 BI NIMO`.
- [x] Перепроверить `TemperatureControl_Init` и допустимый диапазон температур после установки правильного option.
- [x] Повторить проверку `cooling/target`, `cooling/status`, `warmup`.
- [ ] Прогнать приемочный минимум тестов (`pytest`, C# unit, UI smoke).
  - Выполнено частично: `pytest` и `UI smoke` пройдены, `C# unit` не выполнен (в `01_ASCOM_Local_Server` не обнаружены test-проекты).
- [ ] Зафиксировать финальный `Go/No-Go` отчет без блокеров.
